"use client";
// RoleSwitcher is deprecated — role selection is handled by Supabase Auth (user_roles table).
// This component is kept as an empty stub to avoid import errors in any file that still
// references it. It renders nothing.
export default function RoleSwitcher() {
  return null;
}
